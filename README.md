# Restaurant-Java-Program

### properties
- int waitTime
- double rating
- double avgPrice
- double distance
- String hoursOpen
<!--Menu Items (Type?)-->

### what I learned (2/18)
- user interactions (how to take input from users via the Scanner class)
- using getters and setters to make private data accessable but not changable to the main class
- how to use ArrayList (array that you can add and delete from at will) and toString()

### what I learned (3/9)
- how to scan files by creating a file object and copying the path of said file
- how to scan one whole line of text and print it in the same line of code (it comes out as one single line, nothing broken up)
- how to scan each input separately (limit with commas via Delimiter)
- next and nextline for strings only
- how to use format specifiers (used for easy columns)
- to add a restaurant to the “container”
- fix for repetitive execution (loop)
- while statements (what we want to be true)
- hasNext (while the scanner has something next to scan, it will return true)
- scanner will go back to the while statement after curly brace
- can google “how to format string into columns” if unsure how to do something
- how to randomize an arraylist and only print one restaurant
- .length vs .size
- index is 0-61 (because wherever the first is in computer memory, the very first item is pointed to)
- how to get something out of an ArrayList
- use -1 to avoid off by one errors

### what I learned (3/11)

- how to “switch”/check score. A bunch of cases within
- forward dash and two stars for comments
- local variables (third one is for specific return types)
- proper naming/information for other coders
- storing something in memory

